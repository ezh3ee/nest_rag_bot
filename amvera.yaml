meta:
  environment: nodejs
  toolchain:
    name: npm
  version: 22

build:
  skip: false
  additionalCommands: npm run build
  artifacts:
    '*': /

run:
  command: npm run amvera:start
  persistenceMount: /data
  containerPort: '3000'
  servicePort: '80'
  singleton: true
